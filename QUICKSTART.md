# راهنمای سریع شروع 🚀

## 1. دریافت OpenAI API Key

1. به [https://platform.openai.com](https://platform.openai.com) بروید
2. حساب کاربری بسازید یا وارد شوید
3. به بخش API Keys بروید
4. یک API Key جدید ایجاد کنید
5. آن را کپی کنید (فقط یک بار نمایش داده می‌شود!)

## 2. نصب و راه‌اندازی

```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# کپی فایل تنظیمات
cp .env.example .env

# ویرایش فایل .env و وارد کردن API key
# در Windows: notepad .env
# در Linux/Mac: nano .env
```

در فایل `.env` این خط را پیدا کرده و API key خود را وارد کنید:
```
OPENAI_API_KEY=sk-your-api-key-here
```

## 3. اجرا

```bash
python chatbot_web.py
```

## 4. استفاده

مرورگر را باز کرده و به `http://localhost:5000` بروید.

## 5. استقرار در دامنه عمومی

### Render (رایگان)

1. پروژه را در GitHub آپلود کنید
2. به [render.com](https://render.com) بروید
3. New Web Service → Repository را متصل کنید
4. Environment Variables:
   - `OPENAI_API_KEY`: API key شما
   - `SECRET_KEY`: یک رشته تصادفی
5. Deploy!

### Railway

1. به [railway.app](https://railway.app) بروید
2. New Project → Deploy from GitHub
3. Environment Variables را تنظیم کنید
4. Done!

## نکات مهم

- ✅ API key را هرگز در کد قرار ندهید
- ✅ در production حتماً `SECRET_KEY` را تغییر دهید
- ✅ از HTTPS استفاده کنید
- ✅ هزینه API بر اساس استفاده محاسبه می‌شود

## مشکل دارید؟

1. مطمئن شوید Python 3.11+ نصب است
2. همه وابستگی‌ها نصب شده باشند
3. API key معتبر باشد
4. اتصال اینترنت برقرار باشد

